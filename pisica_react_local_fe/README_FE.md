# pisica_React

2) go to root on FE site; run command 'yarn build'; si se creaza automat 'dist' folder
3) copie content de la 'dist' pe hosting
 
2.2)*
Creare director 'dist'

# npm install -g yarn
# yarn install
# npm install nodemailer --save
# npm i --save-dev @types/node
# npm install reactstrap bootstrap # added reactstrap
# npm install axios
# npm install react-scripts --save
